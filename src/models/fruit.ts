export class Fruit {
  id: number;
  no: string;
  description: string;

  constructor(id: number, no: string, description: string) {
    this.id = id;
    this.no = no;
    this.description = description;
  }
}
